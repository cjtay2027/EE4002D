This folder contains the Gaussian Mixture Model(GMM) for simulating a machine as a MDP

Also Contains Custom made Gym Environment which is compatible for API that needs Gym wrapper environment as inputs
